# Make an l-system with a "time" parameter and a "detail" parameter
# For each "time" step the lsystem has to do a predetermined number of "detail" iterations

import lsystem.exec
import mathutils


ex = lsystem.exec.Exec()
# todo
ex.exec(min_iterations=11)

